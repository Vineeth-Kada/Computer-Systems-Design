CHIP Computer{

    IN reset;
    
    PARTS:
        RAM16K(load=false, address=pc, out=instruction);
        CPU(inM = memOut, instruction = instruction, reset = reset, outM= outM, writeM = writeM, addressM[0..5] = addressM,pc=pc);
        RAM64(in=outM, load=writeM, address=addressM, out=memOut);
}