CHIP Computer {
    IN reset;

    PARTS:
    RAM64(in=false, load=false, address=instructionAddress, out=instruction);
    CPU(inM=inM, instruction=instruction, reset=reset, outM=outM, writeM=writeM, addressM=addressM, instructionAddress=instructionAddress);
    RAM64(in=outM, load=writeM, address=addressM, out=inM);
}